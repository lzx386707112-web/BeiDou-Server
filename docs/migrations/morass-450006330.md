# 莫拉斯 450006330 旧端兼容迁移

来源：`/Users/lizixian/Documents/mxd/TMS/MapleStory-IMG/Data`。

本轮迁入莫拉斯“封闭地区4” `450006330`。地图使用现有莫拉斯背景、BGM
和地图标记，并补入 `Obj/morass/closedArea/gate/0,1`、NPC `3003540`、
`3003577`、`3003578` 以及对应地图/NPC 字符串。

`450006330/west00` 保留回到 `450006320/east00` 的普通门。现有
`450006320/portal/1` 已有用户路线改动，本轮不会覆盖；`450006330/door01`
只有剧情脚本、没有可证明的地图目标，因此不迁入。隐藏的剧情 NPC `3003475`
同样不进入普通地图依赖闭包。

所有新 Canvas 均物化外链并转为 GMS ARGB4444：`format=1`、`format2=0`。
既有 `450006320.img` 保持不变；`Obj/morass.img`、`String/Map.img` 和
`String/Npc.img` 只追加白名单原始记录。

静态验证命令：

```bash
rtk python3 tool/scripts/migration/migrate_morass_450006330.py
rtk python3 tool/scripts/audit/audit_morass_450006330.py
```

按本轮要求不做 WZ 打包验证。离线检查通过后，仍需在旧客户端验证启动、登录、
进入 `450006330`、向西回到 `450006320`、NPC 显示、场景动画、BGM 和重复切图。
