# 神秘河完整依赖同步包

本目录包含当前项目中 155 张神秘河地图的完整依赖闭包，而不只是本轮修改文件：

- 客户端：地图、Map 资源、MapHelper、怪物、NPC、BGM、任务、任务物品及
  Map/Mob/Npc/Etc String。
- 服务端：地图、怪物、NPC、任务、任务物品、两套 String XML、怪物掉落数据库
  迁移，以及已存在的相关 NPC/传送脚本。
- 维护工具：迁移、旧端连接节点修复、消亡旅途资源隔离和完整审计脚本。

## 正确使用方式

1. 将 `Client/Data` **合并覆盖**到当前完整项目的 `clien/Data`。
2. 不要删除项目中原有的非神秘河文件。
3. 从合并后的完整项目分别重新打包 `Map.wz`、`Mob.wz`、`Npc.wz`、
   `Sound.wz`、`String.wz`、`Quest.wz` 和 `Item.wz`。
4. 七个 WZ 一起更新。只更新 `Map.wz` 会导致新地图引用的 BGM、怪物、NPC、
   任务物品或字符串资源在客户端中缺失。
5. 将 `Server` 合并覆盖到项目的 `gms-server`，然后重启服务端。
6. 确认 Flyway 已执行 `V2.1.42`；否则怪物不会产生新增掉落。

## 重要说明

这个同步包是“神秘河完整依赖闭包”，仍然不是整个游戏所有区域的完整 Data。
不能直接把本目录中的局部 `Client/Data/Map` 单独打成 `Map.wz` 后替换正式客户端；
那样会丢失其他区域原有地图资源。必须先覆盖到当前完整项目，再从完整项目打包。

本次闭包审计结果：155 张地图、84 个怪物、184 个 NPC、16 首 BGM、
27 个地图资源文件、10039 张 Canvas，`errors=0 warnings=0`。

任务与掉落扩展审计：23 个任务、8 个任务物品、83 种怪物基础掉落、
8 条任务限制掉落，NPC/怪物/物品/前置依赖 `errors=0`。

84 种地图实际怪物的客户端与服务端 `info/eva` 已统一为 `100`，避免现代
`EVA 205–930` 在旧端造成 999 命中仍大量 MISS；其他怪物属性和动画未改动。

啾啾村左侧第一张地图 `450002011` 使用旧端水下世界的 `info/swim=1`，角色
掉入下层水域后可以跳跃游泳返回平台；地图地形、VR 边界和传送点未修改。

莫拉斯城镇 `450006130` 已将现代 `info/fieldLimit=1048576` 降级为相邻旧端
地图使用的 `0`，并移除 NPC `3003490` 上无作用的 `forcedZPage/forcedZMass`；
同时移除 100 个只用于现代编辑器对象关联的 `foothold/piece` 字段。碰撞坐标、
对象、背景、传送点、NPC 资源和小地图均未删减。该图独占引用的
`Obj/morass.img/castle_Outside/stone/7/0/foothold` 已将断档编号
`0,1,2,3,5` 修正为连续的 `0,1,2,3,4`，凸包坐标保持不变。

## 无名村稳定性说明

`450001000` 保留完整地图、12 个 NPC、旧端 `connect/rope/0` 绳索和
`pt=3` 光洞，但不加载 `info/bgm` 与 `info/mapMark`。这两个进图时额外加载的
媒体节点已从无名村移除，并由迁移与审计规则锁定，避免全量重跑后再次加入。

无名村的 18 个旧端绳索显示对象必须排在对象层开头：layer 2 编号 `0-11`，
layer 3 编号 `0-5`。旧版批处理曾把它们追加为 `115-126` 与 `49-54`，导致
已实机通过的 E 版被改坏。当前 `450001000.img` 已恢复到 E 版 SHA-256：
`ac6127f16ca8c56bac8db7448ced677c24ca557cbc22bb4ea861104d679d373e`。

## 450001014 稳定性说明

`450001014` 独占使用的怪物 `8641002` 带有 `firstAttack=1`。现代源把远程攻击
元数据放在 `info/attack/0`；原迁移器删除这个现代节点时，没有先把旧端仍需要的
`type=2` 和 `bulletSpeed=300` 投影到 `attack1/info`。结果是地图和怪物能够短暂
显示，但首次远程攻击初始化时缺少旧端契约字段并崩溃。兼容投影现在固定
`attack1/info` 为 `range, ball, hit, type, attackAfter, bulletSpeed`。

现代攻击动画原本还缺少第 `10` 帧。第 `9` 帧的相同 ARGB4444 载荷会继续用于
补齐 `10`，帧顺序固定为 `0-15`，并保留旧端已验证的
`attack1/info/hit/attach=1`。实机证明仅补帧不能消除本次闪退，因此缺帧属于独立
兼容性修正，不再记作该闪退的根因。

同类检查覆盖当时神秘河地图实际引用的 `83` 个怪物。现代源共有 `92` 条攻击定义，
其中 `12` 条同时满足 `type=2`、存在 `bulletSpeed`、并有对应旧端 `ball` 节点：
`8641002`、`8642012-8642015`、`8642021-8642022`、`8644001`、`8644005`、
`8644007-8644008`、`8644010`。这些怪物全部带 `firstAttack=1`，现在统一把源值
投影到对应的 `attack1/info` 或 `attack2/info`；`8642012-8642022` 使用
`bulletSpeed=400`，其余使用 `300`。每个既有客户端 IMG 通过原始记录级补丁仅增加
`type` 和 `bulletSpeed`，不重编码动画、弹体或命中特效。

## 安息洞穴光洞说明

现代数据中的安息洞穴正向出口使用 `pt=7` 脚本型光洞。旧端不支持该脚本契约，
原迁移因此把它们保留为不可见的到达点，导致路线逐图中断。现在整条路线统一投影为
旧端支持的 `pt=2` 普通传送门：

- `450001210/PS00 -> 450001215/PV00`
- `450001215/PS00 -> 450001218/PV00`
- `450001218/PS00 -> 450001219/PV00`
- `450001219/PS00 -> 450001230/PV00`
- `450001219/PS01 -> 450001240/PV00`
- `450001230/PS00 -> 450001262/PV00`
- `450001230/PH00 <-> 450001230/PH01`
- `450001262/PV00 -> 450001230/PS00`
- `450001240/PS00 -> 450001250/PV00`
- `450001250/PCS00 --坠入瀑布--> 450002000/sp`

路线所需的 `450001219`、`450001230`、`450001240`、`450001250`，怪物
`8641012`，以及 NPC `3003140`、`3003143` 已一并纳入依赖闭包。客户端既有 IMG
使用原始记录级增量补丁，未重编码既有兄弟记录。`450001250/PCS00` 原本是
`pt=9` 剧情碰撞脚本：完成现代任务 `34120` 后进入未安装的剧情图 `450002021`。
现在旧端已用负数任务号 `-31416` 补完对应任务，但剧情图仍不存在，因此保留原坐标
`(8,456)` 的瀑布坠落方式，将其投影为旧端 `pt=3` 碰撞传送，直接进入已安装的
啾啾村 `450002000/sp`。
`450001230` 的现代 `PH00/PH01` 是连接地图左右区域的 `pt=10` 内部门；通用降级
曾把它们投影成不可见的 `pt=3`，使右侧出生区域只显示 `PS00` 一个光洞。该地图
现在单独将这对节点投影为可见的 `pt=2`，坐标及同地图互传目标保持不变。
