# 反转城市、塞拉斯与泰涅布利斯非 Boss 地图迁移

来源：`/Users/lizixian/Documents/mxd/TMS/MapleStory-IMG/Data`。

本轮迁入 `132` 张旧端兼容地图：反转城市 `450014010–450014320`
全地区、`450007170`、塞拉斯 `450016xxx` 全地区，以及月之桥
`450009xxx`、苦痛迷宫 `450011xxx`、利曼 `450012xxx` 的非 Boss 地图。
地图依赖闭包包含 `29` 个地图素材 IMG、`53` 个怪物、`114` 个 NPC
引用、`17` 条 BGM 和 `7` 个地图标记。

## Boss 排除

以下战斗、入口或演出地图不在白名单中：

- 月之桥：`450009400`、`450009430`、`450009450`、`450009480`、
  `450009500`
- 苦痛迷宫：`450011990`
- 利曼：`450012200`、`450012210`、`450012250`、`450012500`、
  `450012600`、`450012650`

已有 `450009301`、`450009400`、`450011990` 和 `Bgm49.img` 使用固定
SHA-256 保护，本轮不会重写。四个带 `boss=1` 的利曼剧情友军
`8645051`、`8645053`、`8645057`、`8645058` 只作为普通地图剧情实体
保留；实际 Boss 怪物不在依赖闭包中。

## 旧端兼容

- 所有迁入 Canvas 使用 GMS 键 ARGB4444：`format=1`、`format2=0`，并
  物化现代 Canvas 链接。
- 新地图、素材、怪物、NPC 和 BGM 包是独立新文件；既有
  `MapHelper.img`、地图素材、String IMG 和 BGM IMG 只追加原始属性记录。
- 两只复用怪物 `8645010`、`8645012` 只把客户端和服务端 `eva` 调整为
  `100`。投射怪物 `8644709` 只补旧端已有证据支持的
  `attack1/info/type=2`，不虚构 `bulletSpeed`。
- 高版本脚本门按 TMS 相邻地图反向门证据投影为普通旧端门。万能传送
  增加反转城市、`450007170`、塞拉斯、月之桥、苦痛迷宫和利曼入口，
  不增加 Boss 入口。

生成与审计：

```bash
rtk python3 tool/scripts/migration/migrate_arcane_river_expansion.py
rtk python3 tool/scripts/audit/audit_arcane_river_expansion.py
```

审计覆盖地图闭包、传送门目标、客户端/服务端 XML 一致性、Boss 排除、
共享 IMG 原始记录范围、IMG 完整解析、全部受影响 Canvas 的格式与解码、
BGM 解码，以及生成器二次运行幂等性。

离线验证不能替代旧客户端实测。交付后仍需检查启动、登录、六个传送入口、
区域内往返、刷怪与攻击、地图切换、回城、重复进入，以及 BGM 播放。
